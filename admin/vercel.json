{
  "ignoreCommand": "if git diff HEAD~1 --name-only | grep -q '^admin/'; then exit 1; else exit 0; fi",
  "rewrites": [
    { "source": "/((?!assets/|.*\\.[a-zA-Z0-9]+$).*)", "destination": "/index.html" }
  ]
}
