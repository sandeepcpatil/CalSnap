SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=
# RevenueCat secret (v1) API key — server-side only, used by /sync to read entitlements
REVENUECAT_SECRET_KEY=
# Shared secret configured on the RevenueCat webhook (sent as: Authorization: Bearer <value>)
REVENUECAT_WEBHOOK_AUTH=
ADMIN_SECRET=
PORT=4000
